services:
  epicswebstudio-dev:
    build:
      target: dev
      args:
        GIT_REPO: ${GIT_REPO}
        GIT_TAG: ${GIT_TAG}
    volumes:
      - .:/app # Mount the source code for development
      - /app/node_modules # Prevent host node_modules from interfering
    image: epicswebstudio-dev:${GIT_TAG}
    container_name: epicswebstudio-dev
    network_mode: "host"
    environment:
      - NODE_ENV=development
    depends_on:
      - epicswebstudio-pvapyws
  epicswebstudio-pvapyws:
    build:
      context: ./pvaPyWS
    volumes:
      - ./pvaPyWS:/app
    image: epicswebstudio-pvapyws:${GIT_TAG}
    container_name: epicswebstudio-pvapyws
    network_mode: "host"
    restart: always
    environment:
      EPICS_DEFAULT_PROTOCOL: ${EPICS_DEFAULT_PROTOCOL}
      EPICS_CA_ADDR_LIST: ${EPICS_CA_ADDR_LIST}
      EPICS_PVA_ADDR_LIST: ${EPICS_PVA_ADDR_LIST}
