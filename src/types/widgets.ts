export type PalleteEntry = {
  widgetLabel: string;
  componentName: string;
};

export type Widget = {
  id: string;
  label: string;
  x: number;
  y: number;
  width: number;
  height: number;
  componentName: string;
};
